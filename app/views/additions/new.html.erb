<h1>New Addition to <b><%= @story.title %></b></h1>

<%= render "layouts/errors", :object => @addition %>

<p class="text-center"><%= image_tag("halloween/#{@image.name}.svg", size: "666") %></p>

<p>
  Using the image above as inspiration, generate an addition to the story!
</p>

<%= form_for [@story, @addition] do |f| %>
  <div class="form-group">
    <%= f.label :body, "Make Your Addition (only one sentence!)" %>
    <%= f.text_area :body, class: "form-control", placeholder: "Previous sentence: #{@placeholder}" %>
  </div>
  <div class="form-group">
    <%= f.label :author, "Your Name" %>
    <%= f.text_field :author, class: "form-control" %>
  </div>
  <%= f.hidden_field :image_id, :value => @image.id %>
  <%= f.submit "Submit", class: "btn btn-default center-block" %>
<% end %>

<%= link_to "Back", :back %>
